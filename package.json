{
  "name": "notch",
  "private": true,
  "description": "Notch \u2014 child growth tracking. Monorepo root.",
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "packages/core"
  ],
  "scripts": {
    "web": "npm run dev --workspace web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "ios": "npm run ios --workspace @notch/mobile"
  }
}
